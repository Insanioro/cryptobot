# ValuBot 💎

Telegram-бот для оценки стоимости @username на основе анализа редкости, спроса и рыночных данных.

## Стек технологий

- Python 3.10+
- Aiogram 3.x
- PostgreSQL + asyncpg
- aiohttp (проверка существования юзернеймов)
- python-dotenv

## Структура проекта

```
valubot/
├── config.py           # Конфигурация и переменные окружения
├── main.py             # Точка входа
├── states.py           # FSM состояния бота
├── requirements.txt    # Зависимости
├── database/
│   └── db.py           # Работа с PostgreSQL
├── handlers/
│   ├── basic.py        # Базовые хендлеры (/start, язык, меню)
│   └── valuation.py    # Хендлеры оценки юзернеймов
├── services/
│   └── logic.py        # Бизнес-логика оценки
├── keyboards/
│   └── builders.py     # Клавиатуры и кнопки
└── locales/
    ├── en.json         # English
    ├── ru.json         # Русский
    └── es.json         # Español
```

## Установка

1. Клонируйте репозиторий:
```bash
git clone <repo-url>
cd valubot
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Создайте `.env` файл:
```env
BOT_TOKEN=your_bot_token_from_botfather
ADMIN_ID=your_telegram_id
ADMIN_USERNAME=your_username_without_at
CHANNEL_URL=https://t.me/your_channel

PG_USER=postgres
PG_PASS=your_password
PG_HOST=localhost
PG_PORT=5432
PG_NAME=valubot
```

4. Создайте базу данных PostgreSQL:
```bash
createdb valubot
```

5. Запустите бота:
```bash
python main.py
```

## Функциональность

- 💎 Оценка стоимости Telegram @username
- 🔍 Проверка существования юзернейма через t.me
- 💾 Кэширование оценок в БД (повторные запросы возвращают те же данные)
- 🌐 Мультиязычность (EN/RU/ES)
- 💼 Интеграция с экспертами для продажи
- 📢 Ссылка на канал
- 📈 Анализ редкости, спроса, эстетики и брендинга

## База данных

### Таблица `users`
| Поле | Тип | Описание |
|------|-----|----------|
| user_id | BIGINT | Telegram ID (PK) |
| language | VARCHAR(5) | Язык пользователя |
| join_date | TIMESTAMP | Дата регистрации |

### Таблица `username_valuations`
| Поле | Тип | Описание |
|------|-----|----------|
| username | VARCHAR(33) | Username без @ (PK) |
| structure | VARCHAR(50) | Структура (кол-во символов) |
| category | VARCHAR(50) | Категория |
| rarity | VARCHAR(50) | Класс редкости |
| demand | VARCHAR(50) | Спрос |
| score | VARCHAR(10) | Эстетическая оценка |
| branding | VARCHAR(50) | Брендинговый потенциал |
| price_low | INTEGER | Минимальная цена |
| price_high | INTEGER | Максимальная цена |
| created_at | TIMESTAMP | Дата оценки |

## Визуальный стиль

Бот использует современный визуальный стиль с HTML-форматированием:
- 💎 Отчёты с разделителями и секциями
- 📊 Метрики в виде структурированных блоков
- 🔍 Анимация анализа при загрузке

## Лицензия

MIT
