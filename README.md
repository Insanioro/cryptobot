# ValuBot 🤖

Telegram-бот для оценки стоимости @username на основе анализа редкости, спроса и рыночных данных.

## Стек технологий

- Python 3.10+
- Aiogram 3.x
- PostgreSQL + asyncpg
- python-dotenv

## Структура проекта

```
valubot/
├── config.py           # Конфигурация и переменные окружения
├── main.py             # Точка входа
├── requirements.txt    # Зависимости
├── database/
│   └── db.py           # Работа с PostgreSQL
├── handlers/
│   ├── basic.py        # Базовые хендлеры (/start, /help)
│   └── valuation.py    # Хендлеры оценки
├── services/
│   └── logic.py        # Бизнес-логика оценки
├── keyboards/
│   └── builders.py     # Клавиатуры и кнопки
└── locales/
    ├── en.json         # English
    ├── ru.json         # Русский
    └── es.json         # Español
```

## Установка

1. Клонируйте репозиторий:
```bash
git clone <repo-url>
cd valubot
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Создайте `.env` файл:
```env
BOT_TOKEN=your_bot_token_from_botfather
ADMIN_ID=your_telegram_id

PG_USER=postgres
PG_PASS=your_password
PG_HOST=localhost
PG_PORT=5432
PG_NAME=valubot
```

4. Создайте базу данных PostgreSQL:
```bash
createdb valubot
```

5. Запустите бота:
```bash
python main.py
```

## Функциональность

- 📊 Оценка стоимости Telegram @username
- 🌐 Мультиязычность (EN/RU/ES)
- 💼 Интеграция с экспертами для продажи
- 📈 Анализ редкости, спроса и брендинга

## База данных

Таблица `users`:
| Поле | Тип | Описание |
|------|-----|----------|
| user_id | BIGINT | Telegram ID (PK) |
| language | VARCHAR(5) | Язык пользователя |
| join_date | TIMESTAMP | Дата регистрации |

## Лицензия

MIT
