# 🤖 Telegram Username Valuation Bot

> Профессиональный бот для оценки стоимости Telegram юзернеймов с продвинутой системой аналитики, рассылок и напоминаний.

[![Python](https://img.shields.io/badge/Python-3.12+-blue.svg)](https://www.python.org/)
[![aiogram](https://img.shields.io/badge/aiogram-3.22+-blue.svg)](https://github.com/aiogram/aiogram)
[![PostgreSQL](https://img.shields.io/badge/PostgreSQL-15+-blue.svg)](https://www.postgresql.org/)
[![License](https://img.shields.io/badge/license-MIT-green.svg)](LICENSE)

---

## 📋 Содержание

- [Возможности](#-возможности)
- [Технологии](#-технологии)
- [Быстрый старт](#-быстрый-старт)
- [Конфигурация](#-конфигурация)
- [Архитектура](#-архитектура)
- [Админ-панель](#-админ-панель)
- [API](#-api)
- [Разработка](#-разработка)

---

## ✨ Возможности

### 🎯 Основной функционал
- **Оценка юзернеймов** — анализ стоимости на основе длины, структуры, редкости и спроса
- **Многоязычность** — поддержка русского, английского и испанского языков
- **Кэширование** — быстрые повторные оценки из базы данных
- **Связь с менеджером** — прямая интеграция с Telegram-аккаунтом администратора

### 📊 Система аналитики
- **Детальная статистика** — по дням, неделям, месяцам
- **Отслеживание событий** — все действия пользователей логируются
- **Аналитика по типам** — проверки ников, контакты с менеджером, рестарты бота
- **Экспорт данных** — выгрузка статистики в удобном формате

### 📢 Система рассылок
- **Массовые рассылки** — текст и изображения
- **Предпросмотр** — проверка сообщения перед отправкой
- **Статистика доставки** — отчет об успешных и неудачных отправках
- **Защита от блокировок** — автоматическое управление заблокированными пользователями

### ⏰ Умные напоминания
- **Автоматические напоминания** — мотивация пользователей связаться с менеджером
- **Настраиваемые интервалы** — гибкая конфигурация через админ-панель
- **Отслеживание взаимодействий** — напоминания отправляются только нужным пользователям
- **Двусторонняя синхронизация** — изменения в UI автоматически попадают в .env файл

### ⚙️ Динамические настройки
- **Настройка без перезапуска** — изменения применяются автоматически в течение 10 секунд
- **Админ-панель** — удобный интерфейс для управления параметрами
- **Синхронизация .env** — изменения в файле автоматически попадают в БД и наоборот
- **Валидация** — проверка корректности введенных значений

---

## 🛠 Технологии

### Backend
- **Python 3.12+** — современный асинхронный Python
- **aiogram 3.22+** — мощный фреймворк для Telegram ботов
- **asyncpg** — быстрый асинхронный драйвер PostgreSQL
- **python-dotenv** — управление переменными окружения

### Database
- **PostgreSQL 15+** — надежная реляционная база данных
- **Миграции** — автоматическое создание и обновление схемы
- **Индексы** — оптимизация производительности запросов
- **JSONB** — гибкое хранение метаданных событий

### Architecture
- **FSM (Finite State Machine)** — управление состояниями диалогов
- **Middleware** — проверка прав доступа администраторов
- **Services** — бизнес-логика вынесена в отдельные сервисы
- **Background Tasks** — фоновые задачи для напоминаний и синхронизации

---

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
git clone https://github.com/yourusername/telegram-username-bot.git
cd telegram-username-bot
```

### 2. Установка зависимостей

```bash
# Создание виртуального окружения
python3 -m venv .venv
source .venv/bin/activate  # Linux/Mac
# или
.venv\Scripts\activate  # Windows

# Установка пакетов
pip install -r requirements.txt
```

### 3. Настройка PostgreSQL

```bash
# Установка PostgreSQL (если еще не установлен)
# macOS
brew install postgresql@15
brew services start postgresql@15

# Linux (Ubuntu/Debian)
sudo apt update
sudo apt install postgresql postgresql-contrib

# Создание базы данных
psql postgres
CREATE DATABASE valubot;
\q
```

### 4. Конфигурация

Создайте файл `.env` в корне проекта:

```bash
cp .env.example .env  # если есть пример
# или создайте новый файл
```

Заполните переменные окружения:

```env
# Telegram Bot Configuration
BOT_TOKEN=your_bot_token_here
ADMIN_ID=your_telegram_id
ADMIN_USERNAME=your_telegram_username
CHANNEL_URL=https://t.me/your_channel

# Admin IDs (comma-separated)
ADMIN_IDS=123456789,987654321

# PostgreSQL Configuration
PG_USER=postgres
PG_PASS=your_password
PG_HOST=localhost
PG_PORT=5432
PG_NAME=valubot

# Reminder settings
REMINDER_DELAY_MINUTES=15
REMINDER_CHECK_INTERVAL_MINUTES=1
REMINDER_ENABLED=true

# Notifications
DEFAULT_NOTIFY_NEW_USERS=true
DEFAULT_NOTIFY_ORDERS=true
DEFAULT_NOTIFY_ABANDONED=true
```

### 5. Запуск бота

```bash
python3 main.py
```

Бот автоматически создаст все необходимые таблицы в базе данных при первом запуске.

---

## 🎛 Конфигурация

### Основные параметры

| Параметр | Описание | Значение по умолчанию |
|----------|----------|----------------------|
| `BOT_TOKEN` | Токен Telegram бота от @BotFather | - |
| `ADMIN_ID` | Telegram ID главного администратора | - |
| `ADMIN_USERNAME` | Username администратора без @ | - |
| `ADMIN_IDS` | Список ID администраторов через запятую | - |

### База данных

| Параметр | Описание | Значение по умолчанию |
|----------|----------|----------------------|
| `PG_USER` | Пользователь PostgreSQL | `postgres` |
| `PG_PASS` | Пароль PostgreSQL | - |
| `PG_HOST` | Хост PostgreSQL | `localhost` |
| `PG_PORT` | Порт PostgreSQL | `5432` |
| `PG_NAME` | Имя базы данных | `valubot` |

### Напоминания

| Параметр | Описание | Диапазон |
|----------|----------|----------|
| `REMINDER_DELAY_MINUTES` | Задержка перед отправкой напоминания | 1-1440 мин |
| `REMINDER_CHECK_INTERVAL_MINUTES` | Интервал проверки напоминаний | 1-60 мин |
| `REMINDER_ENABLED` | Включить/выключить систему напоминаний | `true/false` |

### Уведомления

| Параметр | Описание | Значение по умолчанию |
|----------|----------|----------------------|
| `DEFAULT_NOTIFY_NEW_USERS` | Уведомления о новых пользователях | `true` |
| `DEFAULT_NOTIFY_ORDERS` | Уведомления о заказах | `true` |
| `DEFAULT_NOTIFY_ABANDONED` | Уведомления о брошенных корзинах | `true` |

---

## 🏗 Архитектура

### Структура проекта

```
doberbot/
├── config.py                  # Конфигурация и переменные окружения
├── main.py                    # Точка входа, инициализация бота
├── states.py                  # FSM состояния для диалогов
├── requirements.txt           # Зависимости Python
├── .env                       # Переменные окружения (не в Git)
│
├── database/                  # Работа с базой данных
│   ├── db.py                  # Основной класс Database
│   └── models.py              # Dataclass модели
│
├── handlers/                  # Обработчики сообщений и callback
│   ├── basic.py               # Базовые команды (/start, язык)
│   ├── valuation.py           # Оценка юзернеймов
│   └── admin.py               # Админ-панель и статистика
│
├── keyboards/                 # Клавиатуры для интерфейса
│   ├── builders.py            # Динамические клавиатуры
│   └── admin_keyboards.py     # Клавиатуры админ-панели
│
├── services/                  # Бизнес-логика
│   ├── logic.py               # Логика оценки юзернеймов
│   ├── analytics.py           # Сервис аналитики
│   ├── event_logger.py        # Логирование событий
│   ├── notifications.py       # Уведомления администраторов
│   ├── broadcast_service.py   # Массовые рассылки
│   ├── reminder_service.py    # Система напоминаний
│   └── config_sync.py         # Синхронизация настроек
│
├── middleware/                # Промежуточные обработчики
│   └── admin_check.py         # Проверка прав администратора
│
├── locales/                   # Мультиязычность
│   ├── ru.json                # Русский язык
│   ├── en.json                # Английский язык
│   └── es.json                # Испанский язык
│
└── utils/                     # Вспомогательные функции
    └── formatters.py          # Форматирование текста и статистики
```

### База данных

#### Таблицы

```sql
-- Пользователи
users (
    user_id, language, join_date, username,
    is_bot_blocked, last_valuation_date,
    contacted_manager, reminder_sent
)

-- Оценки юзернеймов
valuations (
    id, user_id, username_checked, estimated_price,
    valuation_date, manager_contacted, reminder_sent
)

-- Кэш оценок
username_valuations (
    username, structure, category, rarity,
    demand, score, price_low, price_high
)

-- События для аналитики
events (
    id, user_id, event_type, timestamp, metadata
)

-- Настройки уведомлений
admin_notification_settings (
    admin_id, notify_new_users, notify_orders,
    notify_abandoned_checkouts, abandoned_threshold
)

-- Кэш ежедневной статистики
daily_stats_cache (
    stat_date, total_users, new_users,
    bot_restarts, group_visits, etc.
)

-- Системные настройки
system_settings (
    setting_key, setting_value, setting_type,
    description, updated_at, updated_by
)
```

---

## 👨‍💼 Админ-панель

### Доступ

Команда: `/admin`

Доступ предоставляется только пользователям из списка `ADMIN_IDS`.

### Разделы панели

#### 📊 Статистика

- **Главная статистика** — общий обзор работы бота
- **За период** — статистика за сегодня, вчера, неделю, месяц
- **По событиям** — детальная статистика по типам событий
- **Список пользователей** — просмотр всех пользователей с пагинацией

#### 📢 Рассылка

1. **Выбор типа** — текст или изображение с подписью
2. **Создание сообщения** — ввод текста или загрузка фото
3. **Предпросмотр** — проверка перед отправкой
4. **Подтверждение** — статистика доставки после завершения

#### ⚙️ Настройки системы

**Интервал проверки напоминаний (1-60 мин)**
- Как часто бот проверяет БД на наличие пользователей для напоминаний
- Изменения применяются автоматически в течение 10 секунд
- Записывается в .env файл

**Задержка отправки напоминания (1-1440 мин)**
- Время после оценки, через которое отправляется напоминание
- Применяется сразу при следующей проверке
- Синхронизируется с .env

#### 🔔 Настройки уведомлений

- Уведомления о новых пользователях
- Уведомления о заказах
- Уведомления о брошенных корзинах
- Порог для abandoned checkout

---

## 🔌 API

### Database API

```python
from database.db import db

# Пользователи
await db.add_user(user_id)
await db.set_language(user_id, 'ru')
user_lang = await db.get_language(user_id)

# Оценки
await db.cache_valuation(username, structure, category, ...)
valuation = await db.get_valuation(username)
await db.create_valuation(user_id, username, price)

# События
await db.log_event(user_id, EventType.BOT_START)
events = await db.get_events_by_user(user_id)

# Напоминания
users = await db.get_users_for_reminder(delay_minutes)
await db.mark_reminder_sent(valuation_id)

# Настройки
value = await db.get_system_setting('reminder_check_interval', '1')
await db.set_system_setting('reminder_delay_minutes', '15', admin_id)
```

### Services API

```python
# Аналитика
from services.analytics import AnalyticsService
analytics = AnalyticsService(db)

stats = await analytics.get_main_stats()
today = await analytics.get_today_stats()
period = await analytics.get_period_stats(days=7)

# Рассылки
from services.broadcast_service import BroadcastService
broadcast = BroadcastService(db)

stats = await broadcast.execute_broadcast(
    bot=bot,
    message_text="Hello!",
    photo_file_id=None
)

# Напоминания
from services.reminder_service import ReminderService
reminder = ReminderService(db)

pending = await reminder.check_pending_reminders()
stats = await reminder.process_reminders(bot)

# Синхронизация настроек
from services.config_sync import ConfigSyncService
config_sync = ConfigSyncService(db)

# .env → БД
synced = await config_sync.check_and_sync()

# БД → .env
await config_sync.sync_to_env('reminder_delay_minutes', '15')
```

---

## 💻 Разработка

### Требования

- Python 3.12 или выше
- PostgreSQL 15 или выше
- pip для управления пакетами

### Установка для разработки

```bash
# Клонирование
git clone https://github.com/yourusername/telegram-username-bot.git
cd telegram-username-bot

# Виртуальное окружение
python3 -m venv .venv
source .venv/bin/activate

# Зависимости
pip install -r requirements.txt

# Запуск в режиме разработки
python3 main.py
```

### Добавление новых команд

1. Создайте обработчик в соответствующем файле `handlers/`
2. Добавьте клавиатуру в `keyboards/` если нужно
3. Добавьте локализацию в `locales/*.json`
4. Зарегистрируйте router в `main.py`

Пример:

```python
# handlers/basic.py
@router.message(Command("help"))
async def cmd_help(message: Message):
    user_lang = await db.get_language(message.from_user.id)
    texts = get_texts(user_lang)
    await message.answer(texts['help_text'])
```

### Добавление новых событий

```python
# database/models.py
class EventType:
    NEW_FEATURE = "new_feature"

# Использование
await db.log_event(
    user_id=message.from_user.id,
    event_type=EventType.NEW_FEATURE,
    metadata={"feature_name": "example"}
)
```

### Тестирование

```bash
# Запуск бота в тестовом режиме
python3 main.py

# Проверка логов
tail -f logs.txt

# Проверка БД
psql valubot
SELECT * FROM users LIMIT 10;
SELECT * FROM events ORDER BY timestamp DESC LIMIT 20;
```

---

## 🔒 Безопасность

- ✅ Токен бота хранится в `.env` файле (не в Git)
- ✅ Проверка прав администратора через middleware
- ✅ Валидация пользовательского ввода
- ✅ Защита от SQL-инъекций (параметризованные запросы)
- ✅ Обработка ошибок и исключений
- ✅ Логирование всех административных действий

---

## 📝 Лицензия

Этот проект распространяется под лицензией MIT. См. файл [LICENSE](LICENSE) для подробностей.

---

## 👥 Авторы

- **Developer** - [Your Name](https://github.com/Insanioro)

---

## 🤝 Вклад в проект

Приветствуются pull requests! Для серьезных изменений сначала откройте issue для обсуждения.

1. Fork проекта
2. Создайте feature branch (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в branch (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

---

## 🙏 Благодарности

- [aiogram](https://github.com/aiogram/aiogram) — отличный фреймворк для Telegram ботов
- [asyncpg](https://github.com/MagicStack/asyncpg) — быстрый PostgreSQL драйвер
- Все контрибьюторы проекта

---

<div align="center">

**⭐ Поставьте звезду, если проект был полезен! ⭐**

Made with ❤️ and Python

</div>
